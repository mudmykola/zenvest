specVersion: "v1beta"
serviceId: "vue-zenvest"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "vue-zenvest-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
